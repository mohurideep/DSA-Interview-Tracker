Problem
Given a string s, return True if it is a valid palindrome, False otherwise.

Ignore:
non-alphanumeric characters
case differences

Function signature
def is_palindrome(s: str) -> bool:
    ...

Examples
"A man, a plan, a canal: Panama" → True
"race a car" → False
"" → True

Rules
Must be O(n)
Must be O(1) extra space
No building new strings