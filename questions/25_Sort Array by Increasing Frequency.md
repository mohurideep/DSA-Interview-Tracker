Problem: Sort Array by Increasing Frequency

Given an integer array nums, sort the array in increasing order based on the frequency of values.
If multiple values have the same frequency, sort them in decreasing order of the value.

Return the sorted array.

Function Signature
def frequency_sort(nums: list[int]) -> list[int]:
    ...
Example
Input:
nums = [1,1,2,2,2,3]

Output:
[3,1,1,2,2,2]