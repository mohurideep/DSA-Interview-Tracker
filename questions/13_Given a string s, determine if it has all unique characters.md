Problem
Given a string s, determine if it has all unique characters.

def has_unique_chars(s: str) -> bool:
    ...


Rules:
Case-sensitive
O(n) time
O(1) space if alphabet is fixed, otherwise explain tradeoff
