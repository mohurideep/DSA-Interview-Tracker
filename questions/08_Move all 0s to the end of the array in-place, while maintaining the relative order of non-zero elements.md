Problem

Move all 0s to the end of the array in-place, while maintaining the relative order of non-zero elements.

Function signature
def move_zeros(nums: list[int]) -> None:
    ...

Example
[0,1,0,3,12] → [1,3,12,0,0]

Rule
Do not return anything
Must be O(n)
Must be O(1) space