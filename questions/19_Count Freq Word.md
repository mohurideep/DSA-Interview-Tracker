Problem
Given a list of strings words, return a dictionary that maps each word to the number of times it appears.

Function signature
def word_frequency(words: list[str]) -> dict[str, int]:
    ...

Example

Input:
["apple", "banana", "apple", "orange", "banana", "apple"]
Output:
{"apple": 3, "banana": 2, "orange": 1}

Or

Problem
You are given a log file containing user search queries. Each query is a word. Count how many times each query appears.

Or

After you solve it, interviewer often asks:
“Return the most frequent word.”
or
“Return top-k frequent elements.”